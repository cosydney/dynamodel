<div class="text-center padded">
<h1>Add photos to my profile</h1>
<%= render 'form' %>
<hr>
<div class="container">
  <div class="row">
  <% @mannequin.photos.each do |photo| %>
    <div class="col-xs-6 col-md-4">
        <%= cl_image_tag photo.path, width: 300, crop: :fill %>
    </div>
    <% end %>
  </div>
</div>

</div>
<style>
  .attachinary_container ul {
    list-style-type: none;
  }
  .attachinary_container ul li {
    display: inline;
    position: relative;
  }
   .attachinary_container ul li img {
    margin-top: 15px;
  }
  .attachinary_container ul li a {
      color: transparent;
      margin-left: -16px;
      position: absolute;
      margin-top: 16px;

  }
  .attachinary_container ul li a::after {
      content: "X";
      color: white;
      displ
  }

</style>