<div class="background" <div class="banner" style="background-image: url(<%= image_path 'landing-bg.jpg' %>);">
  <div class="text-center">
    <h1>Joyn Dyna Model</h1>
    <p>I have no clue what Sydney wants me to write here</p>
    <p>But I surely hope you enjoy the Dyna experience</p>
    <br>
    <div class="container">
      <div class="row">
        <div class="col-md-4 col-md-offset-4">
          <h2>Sign up</h2>

          <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
            <%= f.error_notification %>

            <div class="form-inputs">
              <%= f.input :email, required: true, autofocus: true, placeholder: "dyna@mail.com" %>
              <%= f.input :password, required: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
              <%= f.input :password_confirmation, required: true %>
            </div>

            <div class="form-actions">
              <%= f.button :submit, "Sign up" %>
            </div>
          <% end %>

          <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
</div>
